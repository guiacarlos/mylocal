export function Academia() {
    return (
        <section className="sc-page">
            <h1>Academia</h1>
            <p className="sc-stub">
                Stub. Cursos y lecciones en <code>courses</code>/<code>lessons</code>
                (local), chat IA via <code>chat</code> (server, clave Gemini).
            </p>
        </section>
    );
}
